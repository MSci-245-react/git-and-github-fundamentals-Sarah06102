# Git and GitHub fundamentals

## What I learned from lecture 1
From Lecture 1, I learned that an API can be treated like a menu in a restaurant because it acts as a "contract" that defines what the frontend can order from the backend. 
This allows the two parts of an application to communicate using JSON without the frontend needing to know exactly how the backend's "kitchen" handles the data.

```javascript
1+1
typeof 'string'
1+2 === 4
```

```
2
'string'
false
```
